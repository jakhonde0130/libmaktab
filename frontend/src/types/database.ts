/**
 * Placeholder for the generated Supabase database types.
 * Once the schema (Phase 2) is applied to a live project, regenerate with:
 *   supabase gen types typescript --project-id <project-ref> > src/types/database.ts
 */
export type Database = Record<string, unknown>;
